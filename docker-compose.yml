version: '3.4'

services:
  microservices.calculadordejuros.webapi:
    image: ${DOCKER_REGISTRY-}microservicescalculadordejuroswebapi
    build:
      context: .
      dockerfile: src/Microservices.CalculadorDeJuros.WebApi/Dockerfile
